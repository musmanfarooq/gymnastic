import { Typography } from "@mui/material";
import { styled } from "@mui/system";

export const Linked = styled(Typography)({
  padding: 8,
  borderRadius: 8,
  cursor: "pointer",
  marginBottom: 8,
});
